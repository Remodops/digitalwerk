// Optional Playwright‑Stubs (auskommentiert, keine Dev‑Dep installiert)
// import { test, expect } from '@playwright/test'

// test('Seite lädt ohne JS‑Fehler', async ({ page }) => {
//   const errors: any[] = []
//   page.on('pageerror', (e) => errors.push(e))
//   await page.goto('/')
//   expect(errors).toHaveLength(0)
// })

// test('Skip‑Link ist fokussierbar', async ({ page }) => {
//   await page.goto('/')
//   await page.keyboard.press('Tab')
//   const active = await page.evaluate(() => document.activeElement?.textContent)
//   expect(active?.toLowerCase()).toContain('zum inhalt springen')
// })


